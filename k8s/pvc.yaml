apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bandcheck-backend-data
  namespace: bandcheck
  labels:
    app: bandcheck-backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
